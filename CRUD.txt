(@"Server = DESKTOP-2A8U9LA\SQLEXPRESS; Database = Clientes; User Id =DESKTOP-2A8U9LA\klebe ; Password =55424 ;");




                connetionString = @"Server = DESKTOP-2A8U9LA\SQLEXPRESS; Database = Clientes;Trusted_Connection=True";

CRUD
as quatro operações basicas em banco de dados:

Create                  INSERT  - INSERIR
Retrieve (Read)         SELECT  - CONSULTAR
Update                  UPDATE  - ALTERAR
Destroy (Delete)        DELETE  - DELETAR

public partial class Form1 : Form
    {
        SqlConnection conexao;
        SqlCommand comando;
        SqlDataAdapter da;
        SqlDataReader dr;

        string strSQL;
        public Form1()
        {
            InitializeComponent();
        }

        private void label1_Click(object sender, EventArgs e)
        {

        }

        private void BtAdicionar_Click(object sender, EventArgs e)
        {
            try
            { // crear insert
                
                conexao = new SqlConnection (@"Server= DESKTOP-2A8U9LA\SQLEXPRESS; Database=Clientes; Trusted_Connection=True;");
                strSQL = "INSERT INTO Tabela_T(NOME, NUMERO, ID) VALUES (@NOME, @NUMERO, @ID)";
                comando = new SqlCommand(strSQL, conexao);

                comando.Parameters.AddWithValue("@NOME", TxtNome.Text);
                comando.Parameters.AddWithValue("@NUMERO", TxtNumero.Text);
                comando.Parameters.AddWithValue("@ID", TxtID.Text);
                conexao.Open();
                comando.ExecuteNonQuery();
                
            }
            catch (Exception ex)
            {

                MessageBox.Show(ex.Message);
            }
            finally
            {
                conexao.Close();
                comando.Clone();
                conexao = null;
                comando = null;
            }
        }

        private void BtConsultar_Click(object sender, EventArgs e)
        {
            try // consultar SELECT
            {

                conexao = new SqlConnection(@"Server= DESKTOP-2A8U9LA\SQLEXPRESS; Database=Clientes; Trusted_Connection=True;");
                strSQL = "SELECT * FROM Tabela_T WHERE ID= @ID";
                comando = new SqlCommand(strSQL, conexao);

                comando.Parameters.AddWithValue("@ID", TxtID.Text);
                conexao.Open();
                dr = comando.ExecuteReader();

                while (dr.Read())
                        {
                    TxtNome.Text = (string) dr["nome"];
                    TxtNumero.Text = Convert.ToString (dr["numero"]);
                }
            }
            catch (Exception ex)
            {

                MessageBox.Show(ex.Message);
            }
            finally
            {
                conexao.Close();
                comando.Clone();
                conexao = null;
                comando = null;
            }
        }
        private void BtEditar_Click(object sender, EventArgs e)
        {
            try // Alterar UPDATE
            {

                conexao = new SqlConnection(@"Server= DESKTOP-2A8U9LA\SQLEXPRESS; Database=Clientes; Trusted_Connection=True;");
                strSQL = "UPDATE Tabela_T SET NOME = @NOME, NUMERO = @NUMERO WHERE ID = @ID";
                comando = new SqlCommand(strSQL, conexao);

                comando.Parameters.AddWithValue("@NOME", TxtNome.Text);
                comando.Parameters.AddWithValue("@NUMERO", TxtNumero.Text);
                comando.Parameters.AddWithValue("@ID", TxtID.Text);
                conexao.Open();
                comando.ExecuteNonQuery();

            }
            catch (Exception ex)
            {

                MessageBox.Show(ex.Message);
            }
            finally
            {
                conexao.Close();
                comando.Clone();
                conexao = null;
                comando = null;
            }
        }

        private void BtExcluir_Click(object sender, EventArgs e)
        {
            try // DELETE
            {

                conexao = new SqlConnection(@"Server= DESKTOP-2A8U9LA\SQLEXPRESS; Database=Clientes; Trusted_Connection=True;");
                strSQL = "DELETE Tabela_T WHERE ID= @ID";
                comando = new SqlCommand(strSQL, conexao);

                comando.Parameters.AddWithValue("@ID", TxtID.Text);
                conexao.Open();
                comando.ExecuteNonQuery();

            }
            catch (Exception ex)
            {

                MessageBox.Show(ex.Message);
            }
            finally
            {
                conexao.Close();
                comando.Clone();
                conexao = null;
                comando = null;
            }
        }
    }
    }